# API App

Reserved bootstrap location for the future standalone Arky API application.

Today the runtime server still lives under `crates/arky-server`. This directory
exists to make the target architecture explicit and to provide a stable landing
zone for the future application bootstrap layer.
